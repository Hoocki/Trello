spring:
  flyway:
    schemas: roma
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: roma
    hibernate:
      ddl-auto: validate
server:
  error:
    include-message: always